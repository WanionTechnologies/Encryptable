# Encryptable Starter

This module is a POM-only Spring Boot "starter" that aggregates the dependencies required to use Encryptable with Spring Boot applications.

Usage (in Gradle Kotlin DSL):

implementation("tech.wanion:encryptable-starter:1.0.0")